tcp_record	$tree
	$head_near	 params(handle,	int	;
				buffer,	int	;
				count,	int	)

	c	(tcp_mark)	handle	count

more	c	(sense_data)	handle	count
	ly	a
	lr	count
	jyge	deliver

	xorB	TCP_ESTABLISHED,,xi	. can be remote closed
	jnzb	deliver

	$if	1
	ii	YIELD$
	$else
	dl	(2L)
	ii	TWAIT$
	$endif

	j	more

