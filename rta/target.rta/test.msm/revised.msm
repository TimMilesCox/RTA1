
	$include	../def/rta.def

p	$proc
rbyta_a* $name
LIMIT	$set	160
	$do	p()>2,LIMIT	$set	p(3, 1)
	$do	LIMIT>160,	$flag	string too long
	c	(column)	p(1)	p(2)	LIMIT,,xi
	$end

__column	$head_far	 params(basep,	int	;
					bytep,	int	;
					limit,	int)	;
							;
				scalars(rkxy_s,	float,	r)

	lr	limit
	lx	bytep
	jyge	no
	ly	basep
	ex	xtable,,x

no	qpop	r
	fret	0
