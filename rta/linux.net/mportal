#	$1 can be linux otherwise the build is OSX x86

if	[ $1 = "linux" ]
then
	OSWITCH=-DLINUX
else
	OSWITCH=-DOSX
fi

gcc -m32 -funsigned-char -c -DINTEL $OSWITCH -o ../emulator.rel/portal.o ../portal/portal.c

