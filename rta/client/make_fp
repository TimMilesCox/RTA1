
if [[ "$1" == "powerpc" || "$1" == "sparc" ]]
then
BE=-UINTEL
echo	platform $1
else
BE=-DINTEL
echo	platform little-endian
fi

if [ "$2" == "linux" ]
then
OS=-DLINUX
echo	image for Linux
else
OS=-DOSX
echo	image for OSX
fi



gcc -m32 -funsigned-char -o fp $BE $OS ../emulator.rel/argue.o fp.c

