store_counted $tree
	$head_far	 params(s,		long	;
				bytes,		int)	;
							;
			scalars(save_user_a,	float,	a ;
				save_user_r,	float,	r )

	lr, xi		XQT_FRAME
	lx		s
	ly		s+1

	j		test_end
test_frame
	jyge		reframe
acquire	ex		r2atable, y
	ay, xi		1
	dec		bytes
	sal		$word-$byte
	wsr		$byte
	j		test_end

reframe	lcal		frame
	j		acquire
test_end
	tz		bytes
	j		test_frame
	qpop		r
	qpop		a
	fret		0
	$root	

frame	la		y

	$if	$byte=8
	dsr		24
	d, xi		$word/$byte
	$elseif	$byte=12
	dsr		1
	sbr		23
	$else
	$flag	what's this for then?
	$endif

	ax		a
	ly		b
	lret		0
