subset*	$proc
dimension* $proc
*	$record	dimension(1, 1)-1 dimension(1, 1)+1
left	6
right	6
	$root
	$end
*	$record	subset(1, 1) subset(2, 1)
!	$do	3,match(!) dimension subset(!, 1)
	$root
	$end

all*	$proc
*	$record	all()
	0
	0
e	16
?	$do	2,set(?)	subset	all(0, 2) all(0, 2)
v	12
w	12
o	12
	$root
	$end

$(4:04000)
r	all,6	03fff 12 10 11

