1. RTA1 Embedded Target Software
________________________________

1.1 complete datagram reassembly and source fragmentation

	devise a way of testing
	define and devise restriction on accepting fragments

1.2 add ip MIB to MIB-II implementation, at least ipAddr and maybe ipRoute

	also ipNetToMedia because I may insolently and wrongly use at MIB
	to display ARP cache

	(at MIB is meant to be an old version of ipNetToMedia MIB,
	just local host IP Address to LAN address bindings)

1.3 populate more of MIB-II statistical counters. They're reported but they're zero

	see if netSNMP client/manager can cope with number results up to 6 octets
	(48 bits) in size

1.4 complete Very High Volume TCP/IP buffering and devise tests

	this is dedicated network buffering for realtime messaging TCP
	sockets grouped by port number. This is where TCP capacity rises
	to millions of simultaneous incoming connections

1.5 very possibly explore ZeroConf for emulated RTA1

	this may be defeated by the fact that emulated machine's net addresses
	are necessarily alias addresses of the emulator host's interfaces

	both emulated machine and emulator host are currently capable of
	answering ARP for any address used by the emulated machine.

	The idea falls down if ZeroConf has any directed ARP datagrams in it
	because the Zero Configuration traffic needs to be shown to the
	emulated machine (ARP broadcasts are), without the emulator host
	configuring the ZeroConf address to itself

	It is also a conceivable idea, just to give the whole duty of
	ARP back to the emulator host, since it owns the addresses used
	by RTA1

	or alternatively, never to configure RTA1's protocol addresses to
	the emulator host, which then still needs to drop RTA1's traffic
	from the kernel via firewall, to avoid ICMP DUs and TCP_RSTs
	going out from the emulator host

	UPDATE to 1.5
	_____________

        first trials routing from client within same subnet
        as RTA1
        instead of naming emulator host as a gateway to RTA1
        this is initially done on wired network and works very well

        the WiFi currently in use treats intranet traffic as upload
        and drops hundreds of segments, requiring RTA1 to use an
        aggressive resend which can be switched on via telnet

                telnet -s 172.29.7.6 172.29.7.7
                aggress

        this command is a flipflop so repeating it turns aggressive
        resends off again

        to be in the same wired interface subnet the client has an
        address between 172.29.7.16 and 172.29.7.31 on its wired
        ethernet interface. This causes traffic for RTA1s which have
        addresses from 172.29.7.8 to 172.29.7.15 on emulator host
        external interface to go via wire

        RTA1 addresses on WiFi will need to be moved out of this range

        RTA1 has a new routing table entry 172.19.7.16 255.255.255.240
        which goes to the wired interface and is classed as local

        using same-subnet to route to RTA1 instead of emulator host
        as gateway means RTA1 is doing more ARP for real and could
        do ZeroConf. Up to now it has been largely snooping the
        emulator host's ARP traffic into its own cache

        The old method of using emulator host as gateway is still
        available but is more exactly necessary for the older IPFW
        datagram divert service which isolated RTA1 emulated machine
        from ARP

        Now that it has been realised that the available WiFI is
        chaotic junk for developers, the performance on wire is
        very satisfactory. The aggressive resends aren't needed
        on cabled ethernet (or on a WiFi that's any good. Performance
        has been normal and satisfactory on a better WiFi than the
	one currently available).



2. Emulated RTA1 Network Interfacing for Ubuntu and Windows 10
______________________________________________________________

attempt to have complete emulation available on Linux and Windows 10
by means of BPF based network interfacer portal/osserv

historically only a BPF based network interface has worked on every
Apple OSX release, since IPFW is withdrawn and PCAP never worked
completely on some OSX distributions

masmx.7r3A macro-assembled interface filter programs text-generated by
osserv are in any case more than twice as efficient as PCAP-compiled filters

Ubunto is expected to have BPF. It's hoped a complete BPF API can be
found in the independent software product Windows 10 PCAP

Overall purpose is more exactly to have softcore and eventually fabbed
RTA1 rather than many emulations, but an opportunity is perceived in
having RTA emulated on cheap computers running Windows 10, making use of
the crisp performance of code written in C, compiled with VC++ cl compiler
and running in command prompt

RTA1 might then be shown to many more engineers


3. Documentation
________________

3.1 revise outdated comments at start of target code file t77.4/i_f.msm

3.2 update howto manual on emulated machine and networking
    Document newer single-step display commands

3.3 update manual on smaragd7 RTOS.

